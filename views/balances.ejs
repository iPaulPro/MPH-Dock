<table class="table-striped">
  <thead>
  <tr>
    <th>Coin</th>
    <th>Credited</th>
    <th>Auto Exchange</th>
    <th>On Exchange</th>
  </tr>
  </thead>
  <tbody>
    <% for (let balance of balances) { %>
      <tr>
        <td><a href="https://<%= balance.coin.name %>.miningpoolhub.com/index.php?page=dashboard"><%= balance.coin.code %></a></td>
        <td><%= balance.confirmed %>
          <% if (balance.unconfirmed !== 0) { %>
          <br><span class="hint">(<%= balance.unconfirmed %>)</span>
          <% } %>
        </td>
        <td><%= balance.ae_confirmed %>
          <% if (balance.ae_unconfirmed !== 0) { %>
          <br><span class="hint">(<%= balance.ae_unconfirmed %>)</span>
          <% } %></td>
        <td><%= balance.exchange %></td>
      </tr>
    <% } %>
  </tbody>
</table>