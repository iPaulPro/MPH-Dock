<table class="table-striped">
  <thead>
  <tr>
    <th>Date</th>
    <th>Amount</th>
  </tr>
  </thead>
  <tbody>
  <% for (let credit of credits) { %>
  <tr>
    <td><%= credit.date %></td>
    <td><%= Number(credit.amount).toFixed(8) %></td>
  </tr>
  <% } %>
  </tbody>
</table>